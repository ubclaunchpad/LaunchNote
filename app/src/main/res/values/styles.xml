<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/proxima_nova_alt_regular</item>
    </style>

    <style name="semiBoldTextTab" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:fontFamily">@font/proxima_nova_semibold</item>
    </style>

    <style name="AppBarActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
        <item name="android:fontFamily">@font/proxima_nova_alt_regular</item>
    </style>

    <style name="toolbarTextStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title" />

    <style name="addPhotoTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/proxima_nova_alt_regular</item>
    </style>

    <style name="infoTitleSettings">
        <item name="android:fontFamily">@font/proxima_nova_semibold</item>
        <item name="android:textColor">@color/greyTitle</item>
    </style>

    <style name="infoEditFields">
        <item name="android:fontFamily">@font/proxima_nova_thin</item>
        <item name="android:textColor">@color/greyTitle</item>
    </style>

    <style name="infoButtonSave">
        <item name="android:textColor">@color/save_color_blue</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <!-- <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item> -->
        <!--<item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item> -->
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
        <item name="android:fontFamily">@font/proxima_nova_alt_regular</item>
    </style>

</resources>
